<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mot de passe requis</title>
    <link rel="stylesheet" href="/css/password-prompt.css">
</head>
<body>
    <div class="container">
        <form action="/{{shortId}}/verify" method="POST">
            <input type="password" name="password" placeholder="Entrez le mot de passe" minlength="4" maxlength="6" required>
            {{#errorMessage}}
                <div class="error">{{errorMessage}}</div>
            {{/errorMessage}}
            <button type="submit">Accéder</button>
        </form>
    </div>
</body>
</html>
